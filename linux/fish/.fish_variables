# Use nvm on start
nvm use default --silent

# Fix backtick key in keyboard
xmodmap -e 'keycode 49 = grave asciitilde'

# Make VIM default editor
set --export EDITOR 'vim -f'

# Make VIM default for git
set --export GIT_EDITOR 'vim -f'

# RVM default
rvm default
